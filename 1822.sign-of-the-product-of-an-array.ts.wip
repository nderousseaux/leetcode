/*
 * @lc app=leetcode id=1822 lang=typescript
 *
 * [1822] Sign of the Product of an Array
 */

// @lc code=start
function arraySign(nums: number[]): number {
    return nums
        .map(v => v > 0 ? 1 : v < 0 ? -1 : 0)
        .reduce((p, c) => p * c, 1 as number)
    ;
};
// @lc code=end

