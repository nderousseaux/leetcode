#
# @lc app=leetcode id=1437 lang=python3
#
# [1437] Check If All 1's Are at Least Length K Places Away
#

# @lc code=start
class Solution:
    def kLengthApart(self, nums: List[int], k: int) -> bool:
        indexes = [i for i in range(len(nums)) if nums[i] == 1]
        dif = [indexes[i+1] - indexes[i] for i in range(len(indexes)- 1)]
        return all(d > k for d in dif)
# @lc code=end

